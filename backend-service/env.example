# Database
# For RDS/Production: Add connection pool parameters to handle concurrent connections
# Example: postgresql://username:password@host:5432/hipet?schema=public&sslmode=require&connection_limit=20&pool_timeout=30&connect_timeout=10
# For local development: postgresql://username:password@localhost:5432/hipet
DATABASE_URL="postgresql://username:password@localhost:5432/hipet"

# Database Connection Pool Settings (optional, will be appended to DATABASE_URL if not present)
# Recommended for production: connection_limit=20, pool_timeout=30, connect_timeout=10
# These help prevent "connection pool timeout" errors when worker processes multiple jobs
DATABASE_CONNECTION_LIMIT=20
DATABASE_POOL_TIMEOUT=30
DATABASE_CONNECT_TIMEOUT=10

# Redis
REDIS_URL="redis://localhost:6379"

# JWT
JWT_SECRET="b0e9157a64b165d4059be11da79b200dc60e0349b506e2cb9374ed335b3f1ee4c22240c6d305d7ac3d3925ce907a0559e58108d676588cba927cfee2922a0f43"

# API Configuration
PORT=8000
NODE_ENV=development
API_BASE_URL="http://localhost:8000"
FRONTEND_URL="http://localhost:3000"

# AI Agent Service
AGENT_SERVICE_URL="http://localhost:8001"
AGENT_MODEL="genini"

# Analysis Worker Configuration
# Worker polling interval (milliseconds)
# Production: 30000-60000 (30-60 seconds) to reduce database connection pressure
# Development: 5000-10000 (5-10 seconds) for faster testing
ANALYSIS_WORKER_INTERVAL_MS=30000
# Maximum backoff interval when no jobs available (milliseconds)
# When idle, worker will gradually increase polling interval up to this limit
ANALYSIS_WORKER_MAX_MS=300000
# Maximum retry attempts for failed analysis jobs
ANALYSIS_MAX_ATTEMPTS=3

# Email Service (Placeholder - AWS SES)
AWS_SES_REGION="us-east-1"
AWS_SES_ACCESS_KEY_ID="your-aws-access-key"
AWS_SES_SECRET_ACCESS_KEY="your-aws-secret-key"
AWS_SES_FROM_EMAIL="noreply@yourdomain.com"

# Google OAuth
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# File Upload (Placeholder - AWS S3)
AWS_S3_BUCKET="your-s3-bucket"
AWS_S3_REGION="us-east-1"
AWS_S3_ACCESS_KEY_ID="your-aws-access-key"
AWS_S3_SECRET_ACCESS_KEY="your-aws-secret-key"
